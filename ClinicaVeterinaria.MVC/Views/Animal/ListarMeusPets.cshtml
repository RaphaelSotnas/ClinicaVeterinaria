@model IEnumerable<ClinicaVeterinaria.Web.Models.Entidades.Animal>

@{
    ViewData["Title"] = "ListarMeusPets";
}

<h1>ListarMeusPets</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeAnimal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdadeAnimal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaAnimal.ToString())
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeAnimal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdadeAnimal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaAnimal.ToString())
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
